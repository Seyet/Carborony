export type ShellBusiness = {
  name: string
  initials: string
  permissions: string[]
  roleName: string
}

export type ShellUser = {
  avatarUrl: string | null
  name: string
  email: string
  initials: string
}
